Contest Manager
===============
To share a folder on a Vagrant box, use:
  config.vm.share_folder("logical_name", "guest/path", "C:\host\path")

Vagrant NFS for heavy file load.

config.vm.forward_port 80, 8080

Vagrant multi-vm for distributed SOAs.
