var siteCode = function() {};

(function(siteCode, document) {
  siteCode.resultElem = document.getElementById('loadResult');
  siteCode.resultElem.innerText = 'Script loaded.';
})(siteCode, document);
